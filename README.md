# React Template with superagent for GH Pages

Template created for starting a static website mainly for GitHub Pages but can be used for other purposes.

#Deployment
`npm run build` will run prior to deploy

### `npm run deploy`